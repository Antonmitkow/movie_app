import 'package:flutter/material.dart';

class MovieDetailedMainInfoWidget extends StatelessWidget {
  const MovieDetailedMainInfoWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
